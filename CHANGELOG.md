# Changelog

本项目的所有重要变更都记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循语义化。

## [3.7.0] - 2026-08-30

### 新增
- 工具箱“以文搜图”补齐已知人物、已知地点和自定义对象快捷入口
- 图片描述可按需使用已知参考资料；默认关闭，并提示准确度与速度取舍
- 事件整理可选启用业务背景，并明确它只影响命名、标签和等级，不改变分组

### 改进
- 页面切换先显示再刷新，图库与历史页仅在数据变化时重建，快速切页会忽略过期扫描
- 图片参考收集逻辑由自动分类和工具箱共用，避免不同功能对同一设置理解不一致
- Windows 中文字体统一为 Microsoft YaHei UI；配置结构损坏时自动备份并恢复可用默认值
- 事件整理会提示 EXIF 时间和低可信文件修改时间的数量

### 修复
- 重复点击当前导航导致页面重新扫描和明显卡顿
- 后台图库扫描完成后覆盖用户刚切换到的新页面
- 合法 JSON 但结构错误的旧配置导致应用启动失败
- 未启用业务背景时仍继续套用旧优化提示词

## [3.6.0] - 2026-08-24

### 新增
- 自定义识别目标升级为“已知对象”：支持名称、描述和 1–5 张参考照片
- 自动分类可将待分类照片与已知对象参考图进行实例级视觉比对
- 工具箱支持按已知对象的多张参考图搜索，参考向量取平均并可融合少量文字提示
- CLIP 图片索引保存图片指纹和向量，后续只编码新增或修改过的图片

### 改进
- 图片搜索不再在点击搜索时悄悄建立全量索引，避免突然占满 CPU/GPU
- 未安装快搜引擎时，逐张视觉模型搜索会先明确提示资源消耗
- 单次分类请求最多携带 8 张参考图，避免已知实体增加后推理时间失控
- 移除启动后抢占解释器的页面预热，设置页继续采用渐进式组件构建

## [3.5.0] - 2026-08-24

### 新增
- EXIF 信息页支持修改拍摄日期、标题、描述、作者、关键词、版权和 0–5 评分
- 元数据首次修改默认备份原图，并使用同目录临时文件原子替换
- 设置页新增通用“自定义识别目标”，内置产品与包装、文件资料、工作设备三个物品目标
- 自定义目标同时接入 AI 分类提示词和工具箱“以文搜图”快捷入口

### 改进
- 人物与地点继续保留参考照片精准识别，物品、场景、动作和业务概念不再受固定类型限制
- EXIF 工具明确限制可安全写入的格式，HEIC/BMP/GIF 保持只读，避免隐式转码

### 修复
- Windows 首帧在 CustomTkinter 主题完成前提前显示，导致启动时组件短暂由黑变白

## [3.4.1] - 2026-08-23

### 新增
- 设置页新增“清晰分拣 / 极简分流 / 自定义”图标切换，自定义图标会复制到稳定的用户数据目录

### 改进
- 默认图标改为更直观的“素材流入分类盒”，并为 Windows 生成完整多尺寸 ICO
- 主操作按钮恢复黑底白字，蓝色只用于导航、选择、进度和链接状态
- Windows 设置独立 AppUserModelID，避免任务栏继续沿用 Python 或旧快捷方式图标

### 修复
- 标题栏图标改为同时提供 16–256px 多尺寸，并保留 Tk 图片引用，避免运行时回退默认图标

## [3.4.0] - 2026-08-20

### 新增
- 重新设计“散乱素材 → 智能分拣 → 整齐归位”的跨平台应用图标
- 仪表盘新增隐私安全的使用报告导出，只汇总任务数、处理量和分类结果

### 改进
- Windows 中文界面统一使用 Microsoft YaHei UI，日志使用平台原生等宽字体
- 主导航、仪表盘和主要操作移除跨平台表现不一致的彩色 Emoji
- 页面改为首次访问时创建，减少冷启动期间不必要的界面构建
- 重复文件从永久删除改为移入系统回收站
- AI 状态使用低饱和状态胶囊，减少离线状态对主操作的视觉干扰

### 修复
- Ollama 模型拉取中断或返回错误时仍被误报为下载成功
- 同一个分类引擎停止后再次运行仍保持停止状态
- 增量处理只按文件名判断，可能误跳过不同目录中的同名图片；升级后首次运行会按内容建立来源索引
- Windows 时钟精度下快速连续写入历史记录仍可能产生重复 ID

## [3.3.1] - 2026-08-20

### 改进
- 恢复旧版 Mac 的 Apple 蓝主交互色，选中态改为淡蓝，琥珀色专用于 AI 状态
- Windows、macOS 和 Linux 启动/打包说明统一到 3.3，并补齐依赖失败校验
- 示例内容改为通用分类，移除开发机绝对路径和明文令牌操作建议

### 修复
- CLIP 批次编码失败后图片路径与向量错位
- 规则目标目录可越出输出目录、复制覆盖同名文件及 Windows 非法重命名字符
- 历史记录损坏导致页面打不开、秒级记录 ID 冲突和非原子写入
- 配置防抖保存与主动保存并发写入同一临时文件
- 图片打开后文件句柄未及时释放，以及分类结果记录源路径而非实际输出路径

## [3.3.0] - 2026-08-19

### 新增
- GitHub Actions 跨平台测试矩阵（Windows/macOS/Linux，Python 3.10/3.12）
- Windows PyInstaller 打包冒烟测试与构建产物上传
- 单一版本号来源，界面标题、页脚和开发者面板保持一致

### 修复
- 配置、历史、日志、参考照片和缓存迁移到系统用户目录，安装到 Program Files 后仍可写入
- PyInstaller 不再打包整个 `data/`，防止个人配置、照片和缓存进入分发包
- Windows 启动脚本检查全部必需依赖，不再只检查 customtkinter
- Windows 中文环境下配置测试因默认 GBK 编码失败
- 默认分类与业务背景改为通用示例，人物/地点识别默认关闭

## [3.2.0] - 2026-08-16

### 新增
- 仪表盘重设计：问候头部 + Ollama 在线状态胶囊（点击重试）+ 上次整理摘要 + 近 7 日处理量趋势柱状图
- 测试套件：26 个 pytest 用例覆盖配置/事件聚类/命名模板/断点/缓存/临时文件清理/历史记录
- GitHub Actions CI：双系统（ubuntu/macos）× 双 Python（3.10/3.12）矩阵跑测试与语法检查
- README 增加 Tests/Python/License 徽章；新增 requirements-dev.txt

### 修复
- 配置文件损坏时启动崩溃 → 自动备份损坏文件并回退默认值（测试发现的真 bug）
- config.set 防抖 0.5s + 退出前 flush 强制落盘，连续设置不再每次写磁盘
- 关键词映射缓存、参考照片编码缓存（mtime+size 失效），批量分类提速
- VTracer 矢量路径误删用户原图的严重 bug
- HEIC 临时文件清理补全（encode/缩略图/EXIF/格式转换/日期修正/矢量回退）

## [3.2.2] - 2026-08-17

### 新增
- 双进度条设计：事件模式新增「当前事件」+「总进度」双进度条，总进度用琥珀色区分
- 侧栏全局任务指示器：后台任务运行时侧栏显示状态+进度，切换页面也能看到任务在跑
- EventPipeline.run() 新增 overall 回调参数，报告事件级总进度（evt_num/total_evts）
- 内容分类模式进度也同步到侧栏指示器

### 修复
- 仪表盘 Canvas 字体 Helvetica → TkDefaultFont，修复 Windows/Linux 不兼容
- CI 安装步骤补 openpyxl + tkinterdnd2
- config.py `import threading` 移至文件顶部；_debounced_save 异常写入日志不再静默吞
- 开发者面板点击 5 次无效：CTkLabel → CTkButton 伪装为文字标签；`self.after` → `self.root.after`
- safe_after 通过 `winfo_toplevel().after()` 调度，修复 CTkFrame 无 after 方法导致后台线程回调不执行
- AI 状态检测优化：TCP socket 预探测（毫秒级失败）+ HTTP 确认，Ollama 离线时不再等 5 秒超时

## [3.1.0] - 2026-08-16

### 新增
- 拖拽导入：文件夹直接拖进窗口即设为素材路径（可选依赖 tkinterdnd2，run 脚本自动安装；未安装不报错）
- 事件整理撤销：完成后 30 秒内可一键删除本次输出文件（原文件不受影响，复制式整理天然安全）
- Toast 轻提示组件（拖拽反馈等）
- 工具箱新增「批量处理」：批量调整尺寸（最长边/百分比）、旋转、文字水印（位置/透明度可调），默认输出到 `batch_output/` 保护原文件
- 重复检测新增「相似图片」模式：dHash 感知哈希，可检出截图轻微变化/压缩/缩放后的近似图
- 统一日志：界面日志同时写入 `data/logs/snapsort.log`（滚动保留，200KB×3）
- AI 分类失败自动降级并记录到日志（错误边界）
- 开发者面板补全项目结构与图标修改指引（图标位于 `data/snapsort_icon.png`）

### 修复
- 素材库缩略图 `PhotoImage` 改为主线程创建，修复偶发图片消失/崩溃；缩略图持有引用防 GC
- 格式转换/日期修正日志区高度异常（180/160 行 → 10 行）
- 按事件整理页、工具箱页长内容可滚动
- 智能助手输入框改为初始 4 行、随内容自适应增高（上限 15 行）
- 图片问答支持常用问题快捷标签、回车发送、清空对话
- 重复检测结果区初始隐藏，检出重复后再展开
- **线程安全**：事件整理/内容分类 8 处工作线程回调改用 `_safe_after()`，任务运行中关闭窗口不再触发 `RuntimeError` 崩溃
- **配置原子写入**：`config.json` 改为先写临时文件再 `os.replace`，写入中断不再导致配置损坏丢失

### 性能
- 分类关键词映射表按配置内容缓存，不再每张图片重建
- 人物/地点参考照片 base64 编码结果缓存（按 mtime+size 失效），开启人像识别后批量分类明显提速
- 仪表盘输入/输出目录统计移入后台线程，大目录不再卡死界面

### 修复（数据安全）
- ⚠️ 修复位图转矢量 VTracer 路径误删用户原图的严重 bug（清理临时文件时错误地 unlink 了原文件路径）
- HEIC 临时转换文件的清理补全：`encode_image`/缩略图/EXIF 读取/格式转换/日期修正/矢量回退全部路径不再遗留临时文件
- 工具箱 EXIF 信息页"格式"栏显示 True/False 的显示错误
- 线程池单张处理失败不再中断整批任务（记录警告并继续）

### 代码质量
- `_safe_after()` 三处重复实现合并为 `ui/widgets.safe_after()` 公共函数
- 移除 `toolbox.py` 内联 `__import__("re")`，统一顶部导入

## [3.0.0] - 2026-08-15

### 新增
- 「安静质感」配色系统：纯黑主按钮 + 琥珀色点缀 + 中性灰选中态
- 跨平台字体自动检测（macOS=SF Pro / Windows=Segoe UI / Linux=Noto Sans）
- 多标签预设系统：多套标签自由切换、增删、混用
- 事件合并阈值 `min_photos_per_event`，防止碎片段
- AI Prompt 优化：业务背景 → AI 优化 → 结构化提示词（meta-prompt）
- 结构化输出五字段：SCENE / DESC / PEOPLE / ACTION / MOOD
- 分类失败自动简化 prompt 重试
- 事件模式 3 步引导条（选文件夹 → 配选项 → 看结果）
- 开发者面板：侧栏副标题点击 5 次进入
- CLIP 语义搜图、断点续跑 Checkpoint、本地模型检测与一键下载

### 变更
- 移除所有硬编码用户路径，run.sh / run.bat 完全跨平台

## [2.4.0] - 2026-07

### 新增
- 按事件整理模式：日期分组 → 事件聚类 → AI 命名 → ABC 分级 → 批量重命名
- 批量格式转换（保留 EXIF）、EXIF 查看、日期修正、自定义重命名工具
- 规则引擎：分类后自动化处理（移动/复制/清理）

## [2.3.0] - 2026-07

### 新增
- 位图转矢量重写：Marching Squares 等值线提取 + 贝塞尔平滑，3 种模式
- 智能助手（文本模型文案生成）、MD5 重复检测
- 缩略图磁盘缓存 `data/thumbnails/`
- 模型用途说明与视觉/文本自动切换
- Windows PyInstaller 打包脚本

## [1.0.0] - 2026-06

- 首个可用版本：Ollama 视觉模型自动分类、仪表盘、素材库、历史记录
