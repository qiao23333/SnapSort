# Changelog

本项目的所有重要变更都记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循语义化。

## [3.1.0] - 2026-08-16

### 新增
- 拖拽导入：文件夹直接拖进窗口即设为素材路径（可选依赖 tkinterdnd2，run 脚本自动安装；未安装不报错）
- 事件整理撤销：完成后 30 秒内可一键删除本次输出文件（原文件不受影响，复制式整理天然安全）
- Toast 轻提示组件（拖拽反馈等）
- 工具箱新增「批量处理」：批量调整尺寸（最长边/百分比）、旋转、文字水印（位置/透明度可调），默认输出到 `batch_output/` 保护原文件
- 重复检测新增「相似图片」模式：dHash 感知哈希，可检出截图轻微变化/压缩/缩放后的近似图
- 统一日志：界面日志同时写入 `data/logs/snapsort.log`（滚动保留，200KB×3）
- AI 分类失败自动降级并记录到日志（错误边界）
- 开发者面板补全项目结构与图标修改指引（图标位于 `data/snapsort_icon.png`）

### 修复
- 素材库缩略图 `PhotoImage` 改为主线程创建，修复偶发图片消失/崩溃；缩略图持有引用防 GC
- 格式转换/日期修正日志区高度异常（180/160 行 → 10 行）
- 按事件整理页、工具箱页长内容可滚动
- 智能助手输入框改为初始 4 行、随内容自适应增高（上限 15 行）
- 图片问答支持常用问题快捷标签、回车发送、清空对话
- 重复检测结果区初始隐藏，检出重复后再展开
- **线程安全**：事件整理/内容分类 8 处工作线程回调改用 `_safe_after()`，任务运行中关闭窗口不再触发 `RuntimeError` 崩溃
- **配置原子写入**：`config.json` 改为先写临时文件再 `os.replace`，写入中断不再导致配置损坏丢失

### 性能
- 分类关键词映射表按配置内容缓存，不再每张图片重建
- 人物/地点参考照片 base64 编码结果缓存（按 mtime+size 失效），开启人像识别后批量分类明显提速
- 仪表盘输入/输出目录统计移入后台线程，大目录不再卡死界面

### 修复（数据安全）
- ⚠️ 修复位图转矢量 VTracer 路径误删用户原图的严重 bug（清理临时文件时错误地 unlink 了原文件路径）
- HEIC 临时转换文件的清理补全：`encode_image`/缩略图/EXIF 读取/格式转换/日期修正/矢量回退全部路径不再遗留临时文件
- 工具箱 EXIF 信息页"格式"栏显示 True/False 的显示错误
- 线程池单张处理失败不再中断整批任务（记录警告并继续）

### 代码质量
- `_safe_after()` 三处重复实现合并为 `ui/widgets.safe_after()` 公共函数
- 移除 `toolbox.py` 内联 `__import__("re")`，统一顶部导入

## [3.0.0] - 2026-08-15

### 新增
- 「安静质感」配色系统：纯黑主按钮 + 琥珀色点缀 + 中性灰选中态
- 跨平台字体自动检测（macOS=SF Pro / Windows=Segoe UI / Linux=Noto Sans）
- 多标签预设系统：多套标签自由切换、增删、混用
- 事件合并阈值 `min_photos_per_event`，防止碎片段
- AI Prompt 优化：业务背景 → AI 优化 → 结构化提示词（meta-prompt）
- 结构化输出五字段：SCENE / DESC / PEOPLE / ACTION / MOOD
- 分类失败自动简化 prompt 重试
- 事件模式 3 步引导条（选文件夹 → 配选项 → 看结果）
- 开发者面板：侧栏副标题点击 5 次进入
- CLIP 语义搜图、断点续跑 Checkpoint、本地模型检测与一键下载

### 变更
- 移除所有硬编码用户路径，run.sh / run.bat 完全跨平台

## [2.4.0] - 2026-07

### 新增
- 按事件整理模式：日期分组 → 事件聚类 → AI 命名 → ABC 分级 → 批量重命名
- 批量格式转换（保留 EXIF）、EXIF 查看、日期修正、自定义重命名工具
- 规则引擎：分类后自动化处理（移动/复制/清理）

## [2.3.0] - 2026-07

### 新增
- 位图转矢量重写：Marching Squares 等值线提取 + 贝塞尔平滑，3 种模式
- 智能助手（文本模型文案生成）、MD5 重复检测
- 缩略图磁盘缓存 `data/thumbnails/`
- 模型用途说明与视觉/文本自动切换
- Windows PyInstaller 打包脚本

## [1.0.0] - 2026-06

- 首个可用版本：Ollama 视觉模型自动分类、仪表盘、素材库、历史记录
